export type Keys = {
  up: Phaser.Input.Keyboard.Key;
  down: Phaser.Input.Keyboard.Key;
  left: Phaser.Input.Keyboard.Key;
  right: Phaser.Input.Keyboard.Key;
}

export type KeysConfiguration = {
  up: string | number | Phaser.Input.Keyboard.Key;
  down: string | number | Phaser.Input.Keyboard.Key;
  left: string | number | Phaser.Input.Keyboard.Key;
  right: string | number | Phaser.Input.Keyboard.Key;
}